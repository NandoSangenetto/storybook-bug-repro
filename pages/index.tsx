import Head from 'next/head';

import config from '../helpers/config';

const Home: React.FC = () => (
  <>
    <Head>
      <title>Test</title>
      <link rel="icon" href="/favicon.ico" />
    </Head>
    Test
  </>
);

export default Home;
